# Instructions Agent Vinovalor

- Répondre en français pour ce projet.
- Ne pas prétendre que la marketplace est production-ready tant que Postgres, paiement, conformité alcool et upload sécurisé ne sont pas branchés.
- Préserver `artifacts/` : c'est la base de connaissance de capture.
- Ne pas exposer de secrets dans le code.
- Vérifications minimales avant livraison : `npm run typecheck`, `npm run lint`, `npm run test`, `npm run build`, `npm audit --audit-level=high`, `npm run qa:visual`.
- Les visuels exploitables sont dans `public/media/wines`; les originaux et prompts restent dans `artifacts/visuals`.
